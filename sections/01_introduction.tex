\section{\uppercase{Introduction}}
\label{sec:introduction}
\begin{figure*}[p]
\centering
\includegraphics[width=\textwidth]{activity_diagram.pdf}
\caption{Illustrated activity diagram of the initial incremental study. Nine encodings are evaluated with LPQ and SVM using participant-separated outer folds. Selecting $SAZ$ from the outer scores is exploratory. LR denotes logistic regression and RF random forest. XAI uses the scratch-trained CNN checkpoints responsible for held-out predictions. Later evaluations select encodings within training and add spatial controls and competitive references (Sections~\ref{sec:nested-followup} and~\ref{sec:competitive}). The final illustrations show an aggregate SVM confusion matrix and one CNN explanation from the initial analyses. The tablet and stylus are schematic.}
\label{fig:activity}
\end{figure*}
\noindent
A digitizing tablet records the pen trajectory together with changes in speed, pressure and inclination. Encoding these measurements as colour adds information about the production of a trace while preserving its recognizable structure. We investigate whether these spatially encoded signals improve classification beyond the visible trajectory.

The application is the distinction between Parkinson's disease (PD) and healthy controls (H) in PaHaW, a database of online handwriting and drawing tasks \cite{Drotar2016}. Handwriting exposes aspects of fine motor behaviour, but the relation between pen motion and diagnosis also depends on the task, device and participant characteristics \cite{Impedovo2019}.

We propose a new formulation of \emph{pseudodynamic handwriting minimap signatures}: compact visual representations of task executions, derived from dynamic recordings. The term minimap follows work using compact source-code renderings as visual signatures \cite{Gebara2025}. Here, planar position supplies the image coordinates and colour carries measured pen attributes. A signature denotes this representation, not a person's authentication signature. Starting from a monochrome trace, we add speed, pressure, altitude and azimuth separately, then compare all four three-signal RGB combinations.

Handwriting classification has often used explicitly designed kinematic and pressure features. Drot\'ar et al. compared such features with several classifiers on PaHaW \cite{Drotar2016}. Learned features provide another route, either from pen dynamics \cite{Pereira2016} or from visual attributes of reconstructed writing \cite{Moetesum2019}. Dynamic information has also appeared in earlier handwriting images. Diaz et al. encode movement through acquired points and pen-up trajectories for PD detection \cite{Diaz2019}. Cilia et al. construct synthetic RGB images from handwriting dynamics for Alzheimer's disease detection and compare them with binary images using transfer learning \cite{Cilia2021SyntheticImages}. These precedents establish the broader idea of expressing dynamics visually.

Our formulation combines contact-only minimap rendering, within-recording percentile normalization and circular azimuth centring to map pen signals onto a fixed trajectory. We evaluate its components across all eight PaHaW tasks, using the same Local Phase Quantization (LPQ) descriptor, support vector machine (SVM) search and participant partitions for nine encodings. We then move encoding selection inside training and test controls that change where dynamic values occur along a stroke. All recordings and renderings from a participant remain in the same outer partition.

In the initial comparison, speed--altitude--azimuth raises observed macro F1 on four tasks, including the spiral, from 0.5915 to 0.6716. Its eight-task mean rises from 0.5643 to 0.5752. This ranking motivates the exploratory classifier and XAI analyses; it does not independently evaluate choosing the best encoding. When the encoding choice is evaluated inside training, the task mean is 0.5618, close to the re-evaluated static baseline of 0.5620. Spatial-control comparisons also leave the benefit of the original signal arrangement uncertain after correction.

Related studies do not all combine the same tasks. Casademunt Gonz\'alez examines repeated \textit{l}, \textit{le} and \textit{les}, whereas Drot\'ar and Diaz use different larger subsets \cite{Casademunt2023,Drotar2016,Diaz2019}. We fix the three repeated-pattern tasks before fitting competitive reference methods: a kinematic/pressure SVM and an ImageNet-pretrained ResNet18 with a trained linear head. Both share the participant partitions with our image-based SVMs. Keeping all eight tasks as a secondary comparison tests whether this shorter task set improves decisions for the same people.

On the three fixed tasks, training-selected LPQ+SVM has the highest observed participant-level score (0.6449), while kinematics has the highest task mean. The paired method differences remain uncertain. Task reduction helps the selected-encoding SVM numerically but lowers the static and transfer pipelines. The result concerns the interaction between task set and learning procedure, rather than a universally preferable subset.

The new selection procedures protect outer-test participants, but PaHaW has already informed the research choices. All stages therefore remain development-cohort evaluations. The earlier scratch-trained CNN provides a separate XAI case study: attribution concentrates on writing, yet its spatial agreement with occlusion is weak. Figure~\ref{fig:activity} shows the initial encoding loop and classifier branches; the later controlled evaluations retain those participant partitions.
