\section{\uppercase{Introduction}}
\label{sec:introduction}
\begin{figure*}[p]
\centering
\includegraphics[width=\textwidth]{activity_diagram.pdf}
\caption{Illustrated activity diagram of the incremental study. Nine encodings are evaluated with LPQ and SVM using participant-separated outer folds. Selecting $SAZ$ from the outer scores is exploratory. The classifier branches reuse the participant assignments; LR denotes logistic regression and RF random forest. Task scores and participant voting are distinct endpoints. XAI uses the CNN checkpoints responsible for the held-out predictions. The final illustrations show an aggregate SVM confusion matrix and one previously selected CNN explanation, from different analyses. The tablet and stylus are schematic; the handwriting and results are from the recorded experiments.}
\label{fig:activity}
\end{figure*}
\noindent
A handwritten line shows where the pen travelled. A digitizing tablet also records how quickly the pen moved, how firmly it pressed, and how its inclination changed. These measurements can be encoded in an image while preserving the recognizable structure of the writing. Their value for classification depends on whether the resulting patterns supply discriminative information beyond the visible trajectory.

The application is the distinction between Parkinson's disease (PD) and healthy controls (H) in PaHaW, a database of online handwriting and drawing tasks \cite{Drotar2016}. Handwriting exposes aspects of fine motor behaviour, but the relation between pen motion and diagnosis also depends on the task, device and participant characteristics \cite{Impedovo2019}.

We propose a new formulation of \emph{pseudodynamic handwriting minimap signatures}: compact visual representations of task executions, derived from dynamic recordings. The term minimap follows work using compact source-code renderings as visual signatures \cite{Gebara2025}. Here, planar position supplies the image coordinates and colour carries measured pen attributes. A signature denotes this representation, not a person's authentication signature. Starting from a monochrome trace, we add speed, pressure, altitude and azimuth separately, then compare all four three-signal RGB combinations. This progression makes the contribution of each encoding an explicit experimental question.

Handwriting classification has often used explicitly designed kinematic and pressure features. Drot\'ar et al. compared such features with several classifiers on PaHaW \cite{Drotar2016}. Learned features provide another route, either from pen dynamics \cite{Pereira2016} or from visual attributes of the reconstructed writing \cite{Moetesum2019}. These studies motivate evaluating both a fixed image descriptor and a network that learns from pixels. They do not imply that a learned representation will be preferable with only a few dozen participants per task.

Dynamic information has appeared in earlier handwriting images. Diaz et al. encode movement through acquired points and pen-up trajectories for PD detection \cite{Diaz2019}. Cilia et al. construct synthetic RGB images from handwriting dynamics for Alzheimer's disease detection, comparing them with binary images using deep transfer learning \cite{Cilia2021SyntheticImages}. These precedents establish the broader idea of expressing dynamics visually.

\begin{samepage}
The proposed technique combines contact-only minimap rendering, within-recording percentile normalization and circular azimuth centring to map selected pen signals onto a fixed trajectory. Our methodological contribution is this explicit formulation and its incremental evaluation across all eight PaHaW tasks. The nine encodings share a Local Phase Quantization (LPQ) descriptor, an SVM search and participant assignments. Every recording and rendering from a participant stays in the same outer partition, and model hyperparameters are selected within the training data. The evaluation then extends to classifier choice, task fusion and CNN explanation.\par
\end{samepage}

The speed--altitude--azimuth encoding raises observed macro F1 on four of the eight tasks. The largest increase is on the spiral, from 0.5915 to 0.6716, while the task mean rises from 0.5643 to 0.5752. Losses on the other four tasks and the uncertainty of the paired comparisons leave overall superiority unresolved after multiplicity correction. These results identify where the proposed encoding yields observed gains and where further evidence is needed.

The clearest statistical difference appears in the classifier comparison. In the exploratory comparison on this SVM-selected encoding, LPQ with SVM reaches task-mean macro F1 of 0.5752, compared with 0.5105 for a small CNN trained from scratch; the paired difference survives Holm correction. Logistic regression and Random Forest also exceed this CNN. These results support fixed texture features as a useful baseline for the proposed representation in this cohort.

Combining the available task predictions by majority vote yields participant-level macro F1 of 0.6498 for logistic regression and 0.6488 for SVM. These scores describe one decision per person, a different endpoint from the task mean. For the CNN, held-out Grad-CAM maps concentrate on writing and their highlighted regions affect prediction confidence, yet their median spatial correlation with occlusion is close to zero. The explanation analysis therefore adds a test of agreement to the visual inspection of heatmaps.

\begin{samepage}
The colour encoding carried forward was selected from its cross-validated results on this cohort. The downstream comparison is consequently exploratory, with no independent test of representation selection.
\end{samepage}

Figure~\ref{fig:activity} connects the representation experiments to the classifier and XAI analyses. The loop isolates the encoding comparison; the two later branches reuse its participant partitions.
